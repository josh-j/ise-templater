{
    "AuthorizationProfile": {
        "name": "{{ site.slug }}_Deny",
        "description": "Explicit reject at {{ site.name }}. Site-owned equivalent of the built-in DenyAccess.",
        "accessType": "ACCESS_REJECT",
        "authzProfileType": "SWITCH",
        "profileName": "Cisco"
    }
}
